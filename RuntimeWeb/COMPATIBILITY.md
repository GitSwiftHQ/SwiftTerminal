# Runtime Compatibility Notes

The runtime compatibility guide lives in:

```text
../Documentation/xterm-compatibility.md
```

Keep that guide aligned with `RuntimeWeb/src/main.ts` whenever xterm.js input handling, fitting behavior, `WKWebView` integration, or runtime compatibility rules change.
